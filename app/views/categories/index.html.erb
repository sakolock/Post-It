<%= render 'shared/content_title', title: 'Categories' %>

<ul>
  <% @categories.each do |category| %>
    <li><%= link_to category.name, category_path(category) %> [<%= category.posts.length %>]</li>
  <% end %>
</ul>